# 04 测试用例规范与等价类

标注：课程原讲（用例、等价类、边界值和场景法）；基础补充（数据精度和组合策略）；企业实战（可维护性和回归）；就业重点（用例质量）。

## 1. 本组内容覆盖

- 视频第 5 集：测试用例编辑规范。
- 视频第 6 集：测试用例设计方法之等价类。
- 配套资料：测试用例 Excel、等价类/边界值 Excel、三角形示例。

## 2. 什么是测试用例

测试用例是描述“在什么条件下，输入什么数据，执行什么操作，应该得到什么结果”的可执行测试文档。

好的测试用例应当：

- 可执行：别人拿到后能按步骤复现。
- 可判断：预期结果明确，不使用“应该正常”这类模糊表述。
- 可追溯：能关联需求、模块、版本和 Bug。
- 可复用：回归测试时仍然适用。
- 不重复：尽量用较少用例覆盖更多有效场景。

## 3. 测试用例字段规范

| 字段 | 写作要求 |
| --- | --- |
| 用例编号 | 唯一、稳定、便于检索，例如 `项目_ST_注册_001` |
| 模块 | 标明所属模块/功能，如“用户管理/注册” |
| 用例标题 | 简洁描述测试目的，常用“输入/条件 + 动作 + 结果” |
| 优先级 | 高/中/低，或 P0/P1/P2；核心链路通常为高 |
| 预置条件 | 执行前必须满足的账号、数据、环境和权限 |
| 操作步骤 | 写清路径、数据和动作，步骤可复现 |
| 预期结果 | 与需求一致，可观察、可判定；可一对一或多对一 |
| 实际结果 | 执行后填写真实结果，常用 Pass/Failed/Blocked |
| 备注 | Bug ID、阻塞原因、特殊说明等 |
| 设计者/版本/时间 | 便于责任和版本追踪 |

### 3.1 标题示例

- 不推荐：`测试注册功能`。
- 推荐：`输入有效用户名、有效密码和一致确认密码后注册成功`。
- 推荐：`输入已注册用户名后注册失败并提示用户名已存在`。

### 3.2 步骤与预期结果

推荐每个关键步骤都有对应结果；多个准备步骤共同产生一个业务结果时，可以采用多对一写法，但不能省略关键验证。

## 4. 优先级划分

- **高**：核心业务、主流程、冒烟用例、影响面大的功能。
- **中**：重要异常、边界和常规功能。
- **低**：界面细节、低风险兼容性或非关键辅助功能。

优先级不是严重度。优先级回答“先测什么”，严重度回答“出了问题影响多大”。

## 5. 用例设计的核心原则

- 一个测试点不一定对应一条用例；多个相关测试点可以被一条用例覆盖。
- 但无效等价类通常要尽量一一覆盖，避免一个用例同时引入多个错误原因，导致失败后无法判断是哪条规则的问题。
- 每条用例只验证清晰的目标，避免步骤过长、目标过多。
- 测试数据应具有代表性，并写出关键边界和特殊字符。

## 6. 等价类法

### 6.1 定义

把所有可能的输入划分成若干个子集，同一子集中的数据预期会触发相同或相近的处理逻辑，再从每个子集中选择代表数据测试。

它的价值是把近乎无限的输入空间，压缩成有限、可管理的测试集合。

### 6.2 有效与无效等价类

- **有效等价类**：满足需求约束，系统应接受并正确处理的输入。
- **无效等价类**：不满足约束，系统应拒绝、提示或采取规定异常处理的输入。

### 6.3 分析步骤

1. 从需求中找出约束条件。
2. 针对每个条件划分有效等价类和无效等价类。
3. 为每个等价类编号。
4. 选择有效用例，尽量用最少用例覆盖最多有效类。
5. 对无效类逐一设计用例，通常一次只引入一个主要无效因素。
6. 将组合后的数据、步骤和预期结果写回标准用例表。

### 6.4 为什么等价类能减少用例

假设用户名可以输入任意字符串，理论上输入空间接近无限。若需求规定系统对“纯字母”都采用同一种拒绝逻辑，那么 `abc`、`xingyao`、`testuser` 可以抽象为同一个无效类，选择一个代表值即可先验证该规则。

但“等价”必须建立在处理逻辑相同的假设上。若长度、编码、大小写或前后空格会触发不同逻辑，就应拆成不同的等价类。

## 7. 登录/注册等价类示例

以下示例采用资料中“用户名长度 6~18，且由字母、数字、下划线中的两种或以上组成”的规则。若项目需求是 6~12，应替换长度边界，方法不变。

### 7.1 用户名组合规则

| 类别 | 编号 | 代表数据示例 |
| --- | --- | --- |
| 字母 + 数字 | A1 | `xy8888` |
| 字母 + 下划线 | A2 | `xing_yao` |
| 字母 + 数字 + 下划线 | A3 | `xy_888` |
| 数字 + 下划线 | A4 | `888_666` |
| 纯字母 | B1 | `xingyao` |
| 纯数字 | B2 | `888888` |
| 纯下划线 | B3 | `______` |
| 含其他字符 | B4 | `xy@888` |
| 空值 | B5 | 空字符串 |

### 7.2 用户名长度规则

| 类别 | 编号 | 代表数据 |
| --- | --- | --- |
| 有效长度 6~18 | A5 | 长度 6、7、17、18 |
| 长度小于 6 | B6 | 长度 5 |
| 长度大于 18 | B7 | 长度 19 |

### 7.3 有效用例组合

有效类可以组合，以减少用例数量，例如：

- A1 + A5：字母+数字，长度 6。
- A2 + A5：字母+下划线，长度 18。
- A3 + A5：字母+数字+下划线，长度 7。
- A4 + A5：数字+下划线，长度 17。

无效类建议分别验证 B1~B7；否则一次输入既是纯字母又超长，失败时无法确认系统究竟检查了哪条规则。

## 8. 密码与确认密码的等价类

若需求要求密码必须包含英文和数字、长度固定为 6，可考虑：

- 有效：字母+数字，长度 6。
- 无效：纯字母、纯数字、含不允许字符、长度小于 6、长度大于 6、空值。
- 确认密码：与密码一致、与密码不一致、为空。
- 交互：密码密文显示、复制粘贴策略、错误提示是否清晰。

## 9. 等价类的边界意识

等价类能减少数量，但不应替代边界值测试。对于长度、金额、数量、日期等有明确范围的输入，至少要补充：

- 正好等于下限。
- 刚好小于下限。
- 刚好大于下限。
- 正好等于上限。
- 刚好小于上限。
- 刚好大于上限。

例如长度 6~18，边界重点是 `5、6、7、17、18、19`。第 7 集边界值法会继续展开这一点。

## 10. 边界值法（第 7 集）

### 10.1 定义和核心思想

边界值法是等价类法的补充。它不只从某个输入区间中随便挑一个值，而是优先选择边界附近的数据：

- 正好等于边界：上点。
- 刚好在有效区间内侧：内点。
- 刚好在有效区间外侧：离点。

对闭区间 `[6,18]`，常用集合为 `5、6、7、17、18、19`。其中 `6、18` 是边界上的有效值，`7、17` 是边界内侧值，`5、19` 是边界外侧值。

### 10.2 为什么边界更容易发现缺陷

开发实现中常见错误包括 `<` 写成 `<=`、`>` 写成 `>=`、长度计算少 1、多 1、金额精度处理错误等。这些错误在普通中间值上可能不暴露，在边界值上更容易暴露。

### 10.3 连续数值与离散数据

- 长度为整数时，边界外侧通常是加减 1，例如 5、6、7。
- 金额保留两位小数时，边界外侧应按最小精度变化，例如有效范围 `0.01~200.00`，重点值可为 `0.01、0.02、199.99、200.00、0.00、200.01`。
- 日期、时间、文件大小也要先确定业务精度，再决定“刚刚大一点/小一点”如何表示。

### 10.4 边界值设计步骤

1. 找出所有有范围约束的输入项。
2. 明确上下限是否包含在有效范围内。
3. 根据数据类型确定最小变化单位。
4. 设计上点、内点和离点。
5. 与等价类用例合并，避免重复执行。
6. 为每个边界写清预期结果和错误提示。

## 11. 场景法（第 8 集）

### 11.1 定义

场景法通过业务流程或代码逻辑描述系统可能经过的路径，再为每条重要路径设计用例。它回答的不是“某个输入是否合法”，而是“用户从起点到终点会经历哪些正常和异常流程”。

### 11.2 适用场景

- 注册 -> 登录 -> 选择商品 -> 下单 -> 支付 -> 确认收货。
- ATM 插卡 -> 验证银行卡 -> 输入密码 -> 输入金额 -> 校验余额 -> 出钞。
- 朋友圈发送文本、图片、视频和权限设置等多步骤流程。

### 11.3 流程图基础符号

- 椭圆：开始/结束。
- 矩形：操作步骤。
- 菱形：条件判断或分支。
- 箭头：流程方向；回到上一步表示重试或循环。

### 11.4 正常流和异常流

- **正常流**：从起点按预期条件走到成功终点。
- **异常流**：在某个节点失败、提示、返回、重试或终止。

设计场景时先保证主成功路径，再补充每个判断节点的失败分支和循环上限。

### 11.5 ATM 取款场景示例

| 场景 | 有效银行卡 | 正确密码 | 账户余额足额 | 每日限额足额 | ATM 现金足额 | 预期结果 |
| --- | --- | --- | --- | --- | --- | --- |
| 取款成功 | V | V | V | V | V | 出钞，余额扣减，交易成功 |
| 无效银行卡 | I | V | V | V | V | 提示卡无效并退卡 |
| 密码错误 | V | I | V | V | V | 提示密码错误并允许重试 |
| 三次密码错误 | V | I | V | V | V | 吞卡或锁卡 |
| 账户余额不足 | V | V | I | V | V | 提示余额不足，不出钞 |
| 每日限额不足 | V | V | V | I | V | 提示超出限额，不出钞 |
| ATM 现金不足 | V | V | V | V | I | 提示 ATM 余额不足，不出钞 |

场景法的关键是不要只验证页面提示，还要验证出钞、账户余额、交易流水、卡状态等业务结果。

### 11.6 场景法与等价类的区别

| 对比项 | 等价类 | 边界值 | 场景法 |
| --- | --- | --- | --- |
| 主要对象 | 单个输入项 | 范围边缘 | 多步骤业务流程 |
| 核心问题 | 哪些输入可代表一类数据 | 边界是否处理正确 | 每条业务路径是否走通 |
| 常见结果 | 有效/无效校验 | 接受/拒绝边界值 | 成功、失败、重试、终止 |
| 常见组合 | 与边界值组合 | 补充等价类 | 与流程图、判定表组合 |

## 12. 课程资料中的三角形补充练习

输入三条边 `a、b、c`，可按规则划分测试：

1. 正数约束：`a > 0、b > 0、c > 0`。
2. 能构成三角形：任意两边之和大于第三边。
3. 直角三角形：满足勾股关系。
4. 等腰三角形：恰有两边相等。
5. 等边三角形：三边相等。
6. 还要考虑不构成三角形、零值、负值、小数、极大值和输入类型错误。

测试时要先明确规则优先级，例如“等边三角形”同时满足“等腰”的数学定义，但产品输出分类通常需要规定唯一归类。

## 13. 常见错误

- 把有效等价类和边界值混在一起，导致覆盖关系不清。
- 只写正常输入，不写空值、格式错误和重复数据。
- 一条无效用例同时违反多条规则。
- 预期结果写成“系统正常”，没有写页面、提示、数据和状态变化。
- 需求版本变化后，沿用旧长度或旧字符规则。
- 用例标题重复，无法快速看出区别。
- 只画正常流程，不画错误、重试和返回分支。
- 场景法只验证提示文字，不验证真实业务状态。
- 选择边界时忽略数据精度，例如把金额 `0.011` 当作合法的“两位小数”边界。

## 14. 练习与答案解析

### 练习 1：边界值

**问题**：输入框允许输入整数 1~100，至少应优先测试哪些值？

**答案**：`0、1、2、99、100、101`。

**解析**：`1、100` 是上下边界，`2、99` 是边界内侧，`0、101` 是边界外侧。若系统还有空值、负数、超长数字等规则，还应补充相应无效类。

### 练习 2：金额边界

**问题**：红包金额允许 `0.01~200.00`，最多两位小数。哪些数据比 `0.01、200.00` 更有代表性？

**答案**：有效侧可测 `0.01、0.02、199.99、200.00`；无效侧可测 `0.00、200.01`，并额外验证 `0.001` 这类超过精度的数据。

**解析**：边界的“刚刚大/小一点”必须遵循业务最小精度。`0.011` 不是合法的两位小数，属于精度异常，而不是有效边界内点。

### 练习 3：ATM 场景

**问题**：ATM 取款流程中，密码连续输错 3 次后吞卡。应该至少设计哪些场景？

**答案**：正确密码取款成功、第一次输错后重试成功、第二次输错后重试成功、第三次输错后吞卡、无效卡、余额不足、每日限额不足、ATM 现金不足。

**解析**：不能只写“密码错误”一条。密码错误存在重试次数和状态变化，至少要覆盖 1 次、2 次、3 次三个关键路径；其他余额、限额和 ATM 现金条件是独立异常分支。

### 练习 4：方法选择

**问题**：验证“用户输入用户名后点击注册，成功则创建账号并可登录”，应该使用哪几种方法？

**答案**：用户名规则使用等价类和边界值，注册到登录的业务链路使用场景法，成功后的账号数据还应做关联功能或数据校验。

**解析**：一个需求可以同时使用多种方法。输入校验和业务流程属于不同层次，不能只用一种方法包打天下。

## 15. 练习模板

```text
用例编号：
模块：
标题：
优先级：
预置条件：
操作步骤：
预期结果：
实际结果：
备注：
版本/设计者/时间：
覆盖等价类：
```

## 16. 复习清单

- [ ] 能解释为什么使用等价类法。
- [ ] 能区分有效等价类和无效等价类。
- [ ] 能按步骤完成编号、组合和选例。
- [ ] 能为注册页面写出至少 4 个有效类和 7 个无效类。
- [ ] 能说明等价类与边界值的关系。
- [ ] 能写出一条别人可以直接执行的完整测试用例。
- [ ] 能为整数、金额、日期范围设计边界值。
- [ ] 能画出一个业务流程的正常流和异常流。
- [ ] 能为 ATM 或下单流程列出全部主要场景。
- [ ] 能说明等价类、边界值和场景法各自解决什么问题。

## 17. 就业补充：用例质量的可维护性

企业用例不只看数量，还看是否能长期回归。建议为核心用例增加需求 ID、风险等级、数据准备方式、清理方式和自动化候选标记；需求变更后优先更新追踪关系和受影响用例，而不是复制一份新表。

自动化候选通常是稳定、重复、高频、结果明确的核心回归场景。探索性测试、快速变化页面和强人工判断场景仍应保留手工测试。
