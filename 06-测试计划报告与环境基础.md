# 06 测试计划、测试报告与环境基础

## 1. 视频覆盖

- 第 13 集：测试计划编写。
- 第 14 集：测试报告编写。
- 第 15 集：DOS 命令详情。
- 第 16～17 集：网络体系。
- 第 18 集：Windows 环境搭建。

标注：课程原讲（计划、报告、命令和网络）；基础补充（指标、风险和分层排查）；企业实战（环境模板与发布判断）；就业重点（常见面试问法）。

## 2. 学习目标

- 能把需求范围转成可执行的测试计划。
- 能用数据写测试报告和发布建议，而不是写主观感受。
- 掌握排查测试环境最常用的命令和网络基础。
- 能说明开发、测试、预发布和生产环境的差异。

## 3. 测试计划

### 3.1 测试计划解决什么问题

测试计划回答：测什么、怎么测、谁来测、什么时候测、需要什么环境、有哪些风险、什么条件下结束。

### 3.2 推荐结构

1. 文档信息：项目、版本、作者、日期、变更记录。
2. 项目背景与测试目标。
3. 测试范围：包含和不包含的功能。
4. 测试策略：功能、接口、兼容性、安全、性能、可用性等。
5. 测试类型和优先级。
6. 环境、设备、账号、数据和依赖服务。
7. 人员分工和时间排期。
8. 进入/退出标准。
9. 风险、假设和应对措施。
10. 交付物和沟通机制。

### 3.3 进入与退出标准

**进入标准**：需求和版本基线已确认，测试环境可用，核心账号和数据准备完成，开发自测通过。

**退出标准**：计划用例执行完成，核心用例通过，严重缺陷关闭或有明确豁免，剩余风险已评估，测试报告完成。

退出标准不是“Bug 必须为 0”，而是风险在业务可接受范围内。

### 3.4 风险分析

可用“概率 × 影响”排序：

- 资金、权限、数据一致性和核心交易优先级最高。
- 第三方依赖、数据迁移、并发和发布窗口是常见项目风险。
- 为每个风险指定负责人、预防措施和触发后的应急方案。

## 4. 测试报告

### 4.1 报告结构

- 测试版本和环境。
- 测试范围与未覆盖范围。
- 用例总数、执行数、通过数、失败数、阻塞数。
- 缺陷总数及按严重程度、模块、状态分布。
- 回归结果和遗留风险。
- 兼容性、性能或安全专项结论。
- 发布建议和签字/确认记录。

### 4.2 常用指标

```text
用例执行率 = 已执行用例数 / 计划用例数
用例通过率 = 通过用例数 / 已执行用例数
缺陷修复率 = 已修复缺陷数 / 缺陷总数
严重缺陷遗留率 = 遗留严重缺陷数 / 严重缺陷总数
需求覆盖率 = 已关联用例的需求数 / 需求总数
```

指标必须说明统计口径和时间点，不能脱离范围直接比较。

### 4.3 发布结论写法

推荐写成：“本版本已覆盖登录、订单和支付核心链路；P0/P1 缺陷为 0，剩余 2 个 P2 文案问题不影响主流程；接口超时场景未完成，建议在发布后继续观察。”

避免写“测试完成，质量良好”这类不可验证表述。

## 5. DOS/Windows 常用排查

| 命令 | 用途 |
| --- | --- |
| `cd` / `dir` | 切换目录、查看文件 |
| `mkdir` | 创建目录 |
| `copy` / `move` | 复制、移动文件 |
| `del` | 删除文件，执行前确认路径 |
| `ipconfig` | 查看 IP、网关和 DNS |
| `ping` | 验证基本网络连通性 |
| `tracert` | 查看路由路径 |
| `nslookup` | 排查域名解析 |
| `netstat -ano` | 查看端口和进程 |
| `tasklist` | 查看进程 |
| `taskkill` | 结束异常进程 |

Windows PowerShell 对应常用命令包括 `Get-ChildItem`、`Test-Connection`、`Get-NetTCPConnection`、`Get-Process`。

## 6. 网络基础

### 6.1 必懂概念

- IP：主机在网络中的地址。
- 端口：进程提供服务的逻辑入口。
- DNS：把域名解析为 IP。
- TCP：面向连接、可靠、有序传输。
- UDP：无连接、低开销，可靠性由应用自行保证。
- HTTP/HTTPS：Web 客户端与服务端通信协议；HTTPS 在 HTTP 之上增加 TLS 加密。
- 请求方法：GET、POST、PUT、PATCH、DELETE 等。
- 状态码：2xx 成功、3xx 重定向、4xx 客户端问题、5xx 服务端问题。

### 6.2 测试排查顺序

1. 域名能否解析。
2. IP 是否可达。
3. 端口是否监听。
4. TLS/证书是否正常。
5. HTTP 请求是否到达服务端。
6. 服务端日志、数据库和第三方依赖是否报错。

“能 ping 通”不代表业务接口可用；ICMP、TCP 端口和 HTTP 应用层要分层判断。

## 7. 环境搭建与隔离

- 开发环境：编码和自测，数据不应作为测试结论依据。
- 测试环境：执行系统测试，版本和配置要可追踪。
- 预发布/UAT：尽量模拟生产，做验收和发布前验证。
- 生产环境：真实用户和真实数据，禁止直接做破坏性测试。

环境检查至少包括版本号、服务状态、数据库连接、缓存、消息队列、第三方接口、账号权限、时间同步和日志级别。

## 8. 企业实战：环境问题记录模板

```text
环境：
版本/构建号：
发生时间：
请求地址与端口：
账号与权限：
现象：
客户端证据：
服务端日志：
初步判断：
已尝试措施：
下一步负责人：
```

## 9. 练习与答案解析

### 练习 1：测试报告

**问题**：计划 200 条用例，执行 190 条，其中通过 180 条、失败 6 条、阻塞 4 条。执行率和通过率是多少？

**答案**：执行率为 95%；通过率按已执行口径为 180/190，约 94.74%。

**解析**：通过率分母必须写清楚。有些团队按计划用例统计，此时为 90%；报告中不能只给一个无口径数字。

### 练习 2：网络排查

**问题**：浏览器提示“无法访问网站”，第一步应该做什么？

**答案**：先确认 URL、域名解析和当前环境，再按 DNS、IP、端口、TLS、HTTP、服务日志的层次排查。

**解析**：不要一上来就重启服务。分层排查能区分本机、网络、网关、服务和应用问题。

### 练习 3：发布判断

**问题**：核心支付成功率正常，但存在一个无法复现的 P1 缺陷，是否可以直接发布？

**答案**：不能仅凭“无法复现”发布。需要补充复现条件、日志和影响分析，由产品、开发、测试和负责人共同确认风险及豁免。

**解析**：发布是风险决策，不是测试人员单方面拍板。

## 10. 面试高频问法

### 问：测试计划和测试方案有什么区别？

测试计划偏项目管理，说明范围、资源、进度、角色和风险；测试方案偏技术策略，说明测试类型、方法、数据、环境和具体执行方式。不同公司文档名称可能合并，但内容职责要能区分。

### 问：如何判断测试是否完成？

根据进入/退出标准、范围覆盖、核心链路、缺陷风险、回归结果和未覆盖项综合判断，并在报告中留下证据。

## 11. 动手任务

- 为一个登录注册项目写测试计划。
- 用 `ipconfig`、`ping`、`nslookup`、`netstat` 完成一次分层排查。
- 用真实执行数据写一份一页纸测试报告。
