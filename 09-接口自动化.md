# 09 接口自动化测试

## 1. 视频覆盖

- 第 37～47 集：接口自动化就业行情、Requests、接口实战与关联、Pytest、fixture/conftest、框架封装、YAML、数据驱动、Allure、Jenkins。

标注：课程原讲（Requests、Pytest 和框架）；基础补充（分层、超时和数据隔离）；企业实战（报告、凭据和流水线）；就业重点（自动化取舍与面试）。

## 2. 推荐技术栈

```text
Python + Requests + Pytest + YAML/CSV + Allure + Git + Jenkins
```

核心不是记库函数，而是把测试数据、请求、断言、环境和报告组织成可维护的工程。

## 3. Requests 基础

```python
import requests

response = requests.post(
    "https://test.example.com/api/login",
    json={"username": "demo", "password": "secret"},
    timeout=10,
)
assert response.status_code == 200
assert response.json()["code"] == 0
```

必须设置超时，集中处理 URL、请求头、认证、日志和异常。不要在每条用例中复制一套请求代码。

## 4. 自动化框架分层

```text
tests/       测试用例
api/         接口封装
data/        测试数据
config/      环境配置
utils/       日志、读取器、数据库和通用工具
conftest.py  fixture 与公共前后置
reports/     Allure 等报告
```

推荐分层：测试层表达业务意图，接口层负责请求细节，工具层负责通用能力，配置层负责环境差异。

## 5. Pytest 核心

- 文件名通常为 `test_*.py` 或 `*_test.py`。
- 类名以 `Test` 开头，测试函数以 `test_` 开头。
- 使用 `assert` 表达预期。
- 使用 marker 区分 smoke、regression、slow 等集合。
- 使用参数化复用同一逻辑验证不同数据。

```python
import pytest

@pytest.mark.parametrize("username,expected", [
    ("valid01", 0),
    ("", 1001),
])
def test_login(username, expected, api_client):
    result = api_client.login(username, "pass")
    assert result["code"] == expected
```

## 6. fixture 与 conftest

fixture 用于准备和清理环境、账号、客户端、数据库连接和 token。`conftest.py` 中的 fixture 可被同目录测试自动发现。

```python
import pytest

@pytest.fixture(scope="session")
def api_client(base_url):
    return ApiClient(base_url)
```

作用域选择：function 隔离性强，class/module 减少准备成本，session 适合只读配置或共享客户端。涉及可变业务数据时不要过度共享。

## 7. 接口关联与框架封装

- 登录后保存 token。
- 创建资源后保存 ID。
- 后续请求引用动态值。
- 在失败时输出请求 URL、方法、参数、响应和关联 ID。
- 封装统一的 `get/post/request` 方法，但不要把所有业务逻辑塞进一个万能函数。

## 8. YAML 与数据和代码分离

YAML 适合维护环境、接口数据和测试场景：

```yaml
name: login_success
request:
  method: POST
  path: /api/login
  json:
    username: demo
    password: secret
assertions:
  - path: $.code
    equals: 0
```

要校验字段缺失、类型错误、编码和环境变量替换；复杂逻辑仍应保留在 Python 中，不要把 YAML 变成难以调试的编程语言。

## 9. Allure 报告

报告应展示：用例名称、步骤、请求数据、断言、失败堆栈、附件和环境。失败时自动附加响应摘要和日志，避免报告只有“AssertionError”。

```bash
pytest -m regression --alluredir=allure-results
allure generate allure-results -o allure-report --clean
```

## 10. Jenkins 持续集成

典型流水线：拉取代码 -> 安装依赖 -> 静态检查 -> 执行 smoke -> 执行回归 -> 生成报告 -> 归档结果 -> 通知。

敏感信息使用 Jenkins Credentials 或环境变量；不要把密码、token 和生产地址提交到仓库。

## 11. 质量与维护

- 失败可复现，测试数据可清理。
- 环境可切换，默认指向安全测试环境。
- 用例稳定，不依赖执行顺序。
- 接口封装与业务用例解耦。
- 对外部服务使用 mock 或契约测试降低波动。
- 定期删除失效用例，维护测试集执行时间。

## 12. 练习与答案解析

### 练习 1：超时

**问题**：Requests 请求为什么必须显式设置 `timeout`？

**答案**：避免网络或服务异常时测试进程无限等待，导致流水线挂死；应根据接口类型设置合理超时并记录异常。

**解析**：自动化测试不仅要判断成功，还要能在失败时及时释放资源和提供证据。

### 练习 2：fixture 作用域

**问题**：一个会修改余额的账号准备数据是否适合使用 session 级 fixture？

**答案**：通常不适合。共享可变账号会造成用例相互影响，应使用 function 或独立数据，并在结束后清理。

**解析**：作用域越大，准备成本越低，但状态污染风险越高。

### 练习 3：框架设计

**问题**：为什么不把所有接口写在一个 `test_api.py` 文件中？

**答案**：请求细节、业务意图、数据和公共能力混在一起会难维护、难复用、难定位；应按测试层、接口层、数据层和工具层分离。

**解析**：自动化框架的价值是降低变更成本，而不是增加文件数量。

## 13. 面试高频问法

### 问：接口自动化如何提高稳定性？

控制测试数据和环境，设置超时与重试边界，避免用例依赖顺序，隔离外部服务，失败时保存完整证据，并将 smoke 与回归分层执行。

## 14. 动手任务

- 用 Requests + Pytest 实现登录、查询和创建接口。
- 增加 fixture、参数化、YAML 数据和 Allure 报告。
- 创建 Jenkins 任务执行 smoke 集合。
