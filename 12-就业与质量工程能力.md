# 12 就业与质量工程能力

## 1. 本章定位

本章不是某一集视频的复述，而是结合整门课程和实际岗位要求补充的就业增强内容。目标是把“学过工具”转化为“能够交付质量结果”。

标注：基础补充（质量工程）；企业实战（Git、CI/CD、Docker、可观测性）；就业重点（简历、面试和作品集）；进阶扩展（安全与平台化）。

## 2. 岗位能力分层

### 初级功能测试

需求阅读、测试点、用例、Bug、回归、SQL、接口基础、Linux 基础和团队协作。

### 接口/自动化测试

HTTP、Requests、Pytest、数据驱动、框架分层、报告、Git、Jenkins 和问题定位。

### 性能测试

业务建模、JMeter、Linux 监控、指标分析、瓶颈定位和调优验证。

### 进阶质量工程

CI/CD、容器、可观测性、测试平台、契约测试、服务虚拟化、质量度量和风险治理。

## 3. Git 与协作

```bash
git clone <repo>
git checkout -b feature/api-tests
git add .
git commit -m "test: add login API cases"
git pull --rebase
git push -u origin feature/api-tests
```

工作习惯：小提交、清晰提交信息、提交前运行测试、不提交密钥和本地配置、通过 Pull Request 评审变更。

## 4. CI/CD 中的测试分层

- 提交阶段：静态检查、单元测试、快速 API smoke。
- 合并阶段：接口回归、组件测试和构建。
- 部署到测试环境后：关键 UI、集成测试和数据校验。
- 发布前：完整回归、性能/安全专项和验收。

流水线应让失败尽早暴露，并保存可定位证据，而不是只显示一个红色构建结果。

## 5. Docker 基础认知

测试岗位不一定要求独立设计容器平台，但应能理解镜像、容器、端口、卷、网络和环境变量，并能用 Docker 启动依赖服务、复现问题和搭建测试环境。

安全习惯：不把密钥写进镜像，不使用生产数据构建镜像，固定版本并清理无用容器。

## 6. 测试安全基础

功能测试人员至少应关注：越权、弱密码、敏感信息泄露、SQL 注入、XSS、CSRF、文件上传、重放、速率限制、日志脱敏和会话失效。发现安全问题应按组织流程提交，不在未授权系统进行攻击性验证。

## 7. 简历写法

不要只写“负责功能测试、写测试用例、提交 Bug”。建议使用：

```text
场景/问题 + 采取的测试方法 + 使用的工具 + 发现/解决的风险 + 可验证结果
```

示例：针对支付回调和重复提交设计接口与数据库一致性用例，使用 JMeter/Requests 验证幂等场景，发现支付超时重复生成订单问题，推动增加流水校验和回归用例。

## 8. 面试回答结构

- 概念题：定义 -> 适用场景 -> 示例 -> 易错点。
- 项目题：背景 -> 职责 -> 方法 -> 难点 -> 缺陷 -> 结果。
- 排查题：现象 -> 分层假设 -> 证据 -> 定位 -> 修复 -> 回归。
- 冲突题：先确认目标和事实，再用数据沟通，必要时升级风险，不隐瞒问题。

## 9. 30 天实操路线

1. 第 1 周：测试基础、需求分析、用例、Bug 和 SQL。
2. 第 2 周：HTTP、Postman/JMeter、Linux、接口数据校验。
3. 第 3 周：Requests/Pytest 自动化和 Jenkins。
4. 第 4 周：Selenium、性能基础、项目复盘和面试表达。

每天输出一个可验证成果：测试点表、用例、Bug、SQL、接口脚本、自动化报告或项目讲解录音。

## 10. 练习与答案解析

### 练习 1：简历项目

**问题**：如何把“做过接口测试”写得更有说服力？

**答案**：说明接口范围、鉴权和关联、断言层次、数据/数据库校验、自动化框架、持续集成和实际发现的问题。

**解析**：招聘方关心的是你能否独立交付和定位风险，不是工具名称列表。

### 练习 2：面试排查

**问题**：接口偶发 500，如何回答排查思路？

**答案**：先记录时间、请求 ID、参数和响应；区分是否可稳定复现，再检查网关、应用日志、数据库、下游服务、连接池、超时和资源指标，定位后用回归场景确认修复。

**解析**：好的回答体现分层、证据和闭环，而不是直接猜某一行代码。

### 练习 3：自动化取舍

**问题**：页面每天改版，是否应该马上写大量 UI 自动化？

**答案**：不应马上大量投入。先稳定需求和定位器，优先覆盖高价值核心流程，并把更多规则下沉到接口或服务层测试。

**解析**：自动化投资要考虑维护成本和反馈速度。

## 11. 最终能力检查

- [ ] 能从需求写出测试点和追踪关系。
- [ ] 能用多种方法设计有效、异常和边界用例。
- [ ] 能提交包含证据的 Bug 并设计回归范围。
- [ ] 能使用 SQL、Linux 和日志定位问题。
- [ ] 能完成接口、Web 自动化和基础性能测试。
- [ ] 能把测试接入 Git/Jenkins 并产出报告。
- [ ] 能用业务风险和结果讲清一个项目。
