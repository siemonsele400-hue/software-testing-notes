# 11 性能测试

## 1. 视频覆盖

- 第 59～74 集：性能概念和价值、指标、流程、JMeter 脚本、插件监控、场景设置、非 GUI 压测、Linux 监控、Grafana、瓶颈分析与调优、接口文档和数据驱动。

标注：课程原讲（性能测试和 JMeter）；基础补充（指标、场景和统计）；企业实战（监控、瓶颈和调优）；就业重点（性能项目表达）。

## 2. 学习目标

- 能把业务目标转成性能测试指标和场景。
- 能用 JMeter 构造负载并生成可靠结果。
- 能结合 Linux、监控和日志定位瓶颈。
- 能区分容量、压力、负载、稳定性和基准测试。

## 3. 性能测试类型

- **基准测试**：低负载下建立基线。
- **负载测试**：在预期业务负载下验证指标。
- **压力测试**：逐步增加负载，寻找系统极限。
- **容量测试**：验证系统在目标数据量或用户量下的表现。
- **稳定性测试**：长时间运行，观察资源泄漏、性能衰减和错误累积。
- **峰值/突发测试**：模拟流量突然上升或下降。

## 4. 核心指标

- 并发用户数：同一时间在系统中保持活动的用户。
- 吞吐量：单位时间完成的请求、事务或业务量。
- 响应时间：请求从发出到收到结果的时间，重点看平均值、P90、P95、P99。
- 错误率：失败请求或失败业务占比。
- 资源指标：CPU、内存、磁盘 I/O、网络、线程、连接池、GC。
- 业务指标：下单成功率、支付成功率、每秒交易数和队列堆积。

平均响应时间不能代替分位数；尾部慢请求往往才是用户真正感受到的问题。

## 5. 性能测试流程

```text
目标与范围
-> 业务建模
-> 指标和通过标准
-> 环境与数据准备
-> 脚本开发与调试
-> 基准测试
-> 逐步加压
-> 监控与结果分析
-> 瓶颈定位与调优
-> 回归验证与报告
```

## 6. JMeter 脚本设计

- 线程组定义用户数、启动时间、循环和持续时间。
- HTTP 请求模拟业务接口。
- 关联动态 token、订单号和用户 ID。
- 参数化账号、商品、查询条件和随机数据。
- 断言关键业务结果，避免只看 HTTP 状态码。
- 用事务控制器统计完整业务链路。
- 用定时器模拟真实用户思考时间。

压测前必须先用小流量确认脚本、数据和业务结果正确；脚本错误会把应用问题和测试问题混在一起。

## 7. 场景建模

场景应说明：目标并发、增长曲线、持续时间、业务比例、数据量、通过标准和停止条件。

例如电商场景：浏览 60%、搜索 20%、加购 10%、下单支付 10%，而不是让所有线程只重复登录接口。

## 8. 非 GUI 压测

```bash
jmeter -n -t plan.jmx -l result.jtl -e -o report
```

正式压测关闭 View Results Tree 等图形监听器，使用 JTL、Backend Listener 或监控系统收集数据。压测机本身也要监控，避免压测机成为瓶颈。

## 9. Linux 监控

常用命令：

| 命令 | 关注点 |
| --- | --- |
| `top` / `htop` | CPU、内存、进程 |
| `free -m` | 内存和 swap |
| `df -h` | 磁盘空间 |
| `iostat` | 磁盘 I/O |
| `vmstat` | CPU、内存、上下文切换 |
| `sar` | 历史资源统计 |
| `ss -s` | 网络连接 |
| `pidstat` | 进程级资源 |
| `tail -f` | 实时日志 |

指标必须和时间线对齐：先确定慢请求发生的时间，再对照应用、数据库、网络和系统指标。

## 10. Grafana 与可观测性

Grafana 负责展示监控数据，常与 Prometheus、InfluxDB 等数据源配合。看板应同时展示流量、延迟、错误率和资源，不能只看 CPU。

推荐使用四个黄金信号：延迟、流量、错误和饱和度。应用日志应包含请求 ID，便于从接口追到服务、数据库和下游调用。

## 11. 瓶颈分析

- CPU 高：算法、线程、序列化、加密或 GC 可能是原因。
- 内存高：缓存、对象泄漏、堆配置或请求堆积。
- 磁盘 I/O 高：慢 SQL、日志、排序、临时文件或磁盘性能。
- 数据库瓶颈：索引缺失、锁等待、连接池、慢查询。
- 网络瓶颈：带宽、连接数、TLS、下游响应慢。
- 应用线程池/连接池耗尽：请求排队，延迟和错误率上升。

调优必须先有基线，再一次只改一个主要变量，并用同一场景回归验证。

## 12. 接口文档与随机数据

性能数据应接近真实分布，避免所有线程使用同一账号、商品或订单。随机数据必须可追踪，失败时能根据种子、请求 ID 或数据文件复现。

敏感数据脱敏，压测环境与生产隔离；未经授权不得对第三方或生产系统施压。

## 13. 练习与答案解析

### 练习 1：指标

**问题**：为什么性能报告不能只写平均响应时间？

**答案**：平均值会掩盖尾部慢请求，应同时查看 P90/P95/P99、错误率、吞吐量和资源指标。

**解析**：少数极慢请求可能严重影响体验，分位数更适合描述尾部延迟。

### 练习 2：脚本可信度

**问题**：压测结果很快，但所有请求都使用同一个账号和缓存数据，能否直接下结论？

**答案**：不能。数据分布不真实，可能命中缓存、触发锁竞争或绕开真实业务路径，应重新设计数据和场景。

**解析**：性能测试首先要保证业务模型和数据模型可信。

### 练习 3：瓶颈定位

**问题**：吞吐量下降、P99 升高、CPU 低但数据库连接池满，优先排查什么？

**答案**：优先排查慢 SQL、数据库锁等待、连接未释放、连接池配置和下游数据库响应。

**解析**：CPU 低不代表系统空闲，线程可能在等待数据库连接或 I/O。

## 14. 面试高频问法

### 问：性能测试和功能测试有什么关系？

功能正确是性能测试的前提；性能测试在正确业务链路上施加负载，验证响应、容量、稳定性和资源表现。不能用功能通过替代性能结论。

## 15. 动手任务

- 为登录、查询、下单设计一套混合场景。
- 先做基准测试，再逐步加压并记录 P95、P99、错误率和资源。
- 用 Linux 命令和 Grafana 看板建立时间线。
- 针对一个瓶颈提出假设、改动并回归验证。
