# 08 JMeter 接口测试

## 1. 视频覆盖

- 第 25～36 集：接口测试基础、JMeter 安装与目录、组件和作用域、接口实战、关联、场景、断言、调试、加密/签名、数据驱动、数据库校验、Ant/Jenkins 持续集成。

标注：课程原讲（JMeter 操作）；基础补充（HTTP、断言和数据一致性）；企业实战（调试、命令行和持续集成）；就业重点（接口排查与工具取舍）。

## 2. 学习目标

- 能读懂接口文档并独立发送 HTTP 请求。
- 能用 JMeter 编写可维护的接口测试脚本。
- 能处理登录 token、上下文参数、断言、参数化和数据库校验。
- 能把接口脚本接入命令行和 Jenkins。

## 3. 接口测试基础

接口测试验证服务端在不同请求、数据和状态下是否返回正确结果，并检查数据库、消息、缓存和下游服务的副作用。

### 3.1 请求组成

- URL：协议、域名、端口、路径和查询参数。
- Method：GET 查询、POST 创建/提交、PUT 整体更新、PATCH 部分更新、DELETE 删除。
- Headers：`Content-Type`、`Authorization`、`Accept`、追踪 ID 等。
- Body：JSON、表单、文件、XML 等。

### 3.2 响应验证

- HTTP 状态码。
- 响应头和 Content-Type。
- JSON 字段、类型、业务码和错误信息。
- 响应时间、分页、列表数量和排序。
- 数据库状态、缓存变化、消息和审计记录。

## 4. JMeter 核心结构

```text
测试计划
└── 线程组
    ├── 配置元件
    ├── 前置处理器
    ├── 定时器
    ├── 取样器
    ├── 后置处理器
    ├── 断言
    └── 监听器
```

- **线程组**：定义并发用户数、启动时间和循环次数。
- **HTTP Request**：发送接口请求。
- **HTTP Header Manager**：统一管理请求头。
- **CSV Data Set Config**：从 CSV 读取参数。
- **JSON Extractor**：提取响应中的 token、ID 等值。
- **Response Assertion/JSON JMESPath Assertion**：验证响应。
- **View Results Tree**：调试脚本，正式压测不建议开启。

## 5. 作用域与执行顺序

作用域决定组件影响哪些取样器；常见顺序是配置元件、前置处理器、定时器、取样器、后置处理器、断言和监听器。

经验规则：

- 公共请求头放在线程组或测试计划层级。
- 只服务于单个请求的提取器和断言放在请求下。
- 调试阶段使用监听器，压测时关闭图形监听器。
- 不要把登录、业务请求和断言混成无法定位的大块脚本。

## 6. 接口实战：登录到业务请求

1. 登录接口发送账号密码。
2. 用 JSON Extractor 提取 `token`。
3. 在 Header Manager 中通过 `${token}` 注入认证头。
4. 请求业务接口并提取业务 ID。
5. 后续请求引用业务 ID。
6. 断言状态码、业务码、关键字段和数据结果。

## 7. 关联

关联就是把前一个请求响应中的动态值提取出来，作为后续请求的输入。常见动态值：token、订单号、用户 ID、时间戳、验证码关联值。

示例：

```text
JSON Extractor
JSON Path: $.data.token
Variable: token

Header:
Authorization: Bearer ${token}
```

提取失败时应让用例明确失败，不要让空 token 继续请求造成后续大量假失败。

## 8. 断言设计

至少分三层：

1. **协议层**：状态码、响应头、Content-Type。
2. **业务层**：业务码、提示、关键字段、列表和状态。
3. **数据层**：数据库记录、金额、库存、流水和副作用。

不要只断言 HTTP 200；很多业务错误也可能返回 200。

## 9. 调试方法

- 先用单线程、单循环、少量数据。
- 用 Debug Sampler 查看变量值。
- 通过 View Results Tree 检查请求、响应和提取结果。
- 检查变量作用域、编码、Content-Type 和 URL 拼接。
- 确认失败是请求失败、断言失败还是数据校验失败。

## 10. 加密与签名接口

常见方式：密码加密、AES/RSA、MD5/SHA 摘要、HMAC 签名、时间戳和随机数。

测试重点：

- 签名字段和排序规则。
- 时间戳过期、重放和时钟偏差。
- 密钥不应硬编码进公共仓库。
- 缺字段、字段篡改、重复签名和大小写差异。
- 加密前后编码、Base64、URL 编码和字符集。

JMeter 中可用 JSR223 + Groovy 或插件生成签名；应先用接口文档和后端实现确认算法，不凭猜测拼接。

## 11. 数据驱动与数据库校验

### 11.1 CSV 数据驱动

```text
username,password,expected_code
user01,pass01,0
user02,wrong,1001
```

注意文件编码、变量名、循环策略、空行和数据隔离。并发执行时不要让多个线程争用同一个一次性账号。

### 11.2 数据库校验

接口成功后查询数据库确认记录、状态、金额或流水。校验应包含：

- 唯一键和关联 ID。
- 状态和时间字段。
- 金额精度和汇总。
- 失败事务是否回滚。
- 异步写入是否需要轮询或等待。

测试账号与数据应可重置，SQL 只读优先，禁止直接修改生产数据。

## 12. 命令行与持续集成

JMeter GUI 用于调试，正式执行使用非 GUI 模式：

```bash
jmeter -n -t api-test.jmx -l result.jtl -e -o report
```

Ant 或 Jenkins 中应配置：工作空间清理、参数注入、结果归档、失败阈值、报告发布和敏感变量管理。

## 13. 企业实战检查清单

- 环境地址可配置，不写死在脚本中。
- 公共请求头、账号和密钥集中管理。
- 每个核心接口至少有成功、参数错误、鉴权失败和数据不存在场景。
- 断言覆盖状态码、业务码、关键字段和数据副作用。
- 失败日志包含请求、响应摘要、变量和关联 ID。
- 压测脚本不使用 View Results Tree 等高开销监听器。

## 14. 练习与答案解析

### 练习 1：登录关联

**问题**：登录成功返回 token，后续查询接口需要 token。如何在 JMeter 中实现？

**答案**：用 JSON Extractor 提取 token，保存为变量，在 Header Manager 中配置 `Authorization: Bearer ${token}`，再对查询接口添加状态码和业务结果断言。

**解析**：关联的关键是“提取、保存、引用、校验”四步。

### 练习 2：断言

**问题**：为什么只断言 HTTP 200 不够？

**答案**：HTTP 200 只代表传输层请求完成，业务可能返回失败码、空数据、错误状态或错误金额。

**解析**：接口测试必须同时验证协议层、业务层和数据层。

### 练习 3：持续集成

**问题**：Jenkins 执行 JMeter 脚本时为什么不应使用 GUI？

**答案**：GUI 会消耗大量内存和 CPU，影响结果稳定性；持续集成应使用非 GUI 模式并归档 JTL、HTML 报告和日志。

**解析**：GUI 是开发调试工具，不是批量执行工具。

## 15. 面试高频问法

### 问：接口测试比 UI 测试有什么优势？

接口测试执行快、定位更直接、覆盖数据和异常更灵活，能在 UI 完成前验证服务端；但不能完全替代 UI 的关键用户流程和视觉交互验证。

### 问：如何判断接口自动化值得做？

优先选择稳定、频繁回归、业务价值高、输入输出明确且手工成本高的接口；一次性、频繁变更、强依赖人工判断的接口不宜盲目自动化。

## 16. 动手任务

- 用 JMeter 完成登录、查询、创建、删除一条业务链路。
- 加入 token 关联、参数化和三层断言。
- 用数据库查询验证接口副作用。
- 用命令行执行并生成 HTML 报告。
