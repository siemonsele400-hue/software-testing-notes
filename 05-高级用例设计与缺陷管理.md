# 05 高级用例设计与缺陷管理

## 1. 视频覆盖

- 第 9 集：错误推断法。
- 第 10 集：因果图与判定表法。
- 第 11 集：测试用例评审。
- 第 12 集：Bug 详细讲解。

本章把“会设计用例”推进到“能用较少用例覆盖关键风险，并能让缺陷进入可追踪闭环”。

标注：课程原讲（方法与流程）；基础补充（判定、生命周期与风险）；企业实战（评审、缺陷和回归）；就业重点（面试表达）。

## 2. 学习目标与适用岗位

- 功能测试、Web 测试、业务测试岗位的必学内容。
- 能根据历史缺陷和业务经验补充容易漏测的场景。
- 能处理多条件组合规则，避免只靠等价类和边界值。
- 能独立参加用例评审、提交高质量 Bug 并完成回归判断。

## 3. 错误推断法

### 3.1 定义

错误推断法不是机械公式，而是基于测试人员的经验、业务理解、历史缺陷和常见开发错误，主动猜测系统最可能出错的地方，再把猜测转化为用例。

常见推断来源：

- 过去版本出现过的缺陷。
- 相似模块、相似产品的缺陷。
- 需求中含糊、矛盾或容易误解的描述。
- 空值、重复提交、并发点击、刷新、返回、超时。
- 权限越界、参数篡改、金额精度、时区、编码和大小写。
- 数据库约束、缓存、消息队列和第三方服务失败。

### 3.2 使用步骤

1. 收集历史 Bug、线上事故和用户投诉。
2. 按输入、状态、权限、并发、环境、依赖服务分类。
3. 把“可能出错”写成可执行的操作和数据。
4. 为每个猜测补充明确预期结果。
5. 将高风险错误加入冒烟、回归或专项测试集。

### 3.3 示例

以“下单支付”为例，可以推断：重复点击支付是否产生两笔订单、支付成功但回调延迟是否重复扣款、库存扣减失败是否仍显示支付成功、优惠券过期后是否仍可使用、用户修改价格参数是否被服务端拒绝。

## 4. 因果图与判定表

### 4.1 适用场景

当一个结果由多个条件组合决定时，单纯的等价类难以覆盖组合关系。此时使用因果图和判定表，适合：

- 登录、注册和权限判断。
- 优惠券、促销和会员折扣。
- 贷款审批、风控规则和额度计算。
- 订单状态、库存和支付结果组合。

### 4.2 因果图基本元素

- **原因**：输入条件或前置条件，例如“已登录”“余额足够”。
- **结果**：系统输出或状态变化，例如“允许支付”。
- **恒等**：原因成立导致结果成立。
- **非**：条件取反。
- **或**：多个条件任意一个成立即可。
- **与**：多个条件同时成立才触发。
- **约束**：互斥、包含、唯一、禁止同时成立等业务限制。

实际工作中不必拘泥于图形画得漂亮，重点是把条件、约束、结果和组合关系写完整。

### 4.3 判定表结构

| 部分 | 含义 |
| --- | --- |
| 条件桩 | 所有影响结果的条件名称 |
| 条件项 | 每个条件在规则中的取值，通常为 Y/N/不关注 |
| 动作桩 | 系统可能执行的动作或输出 |
| 动作项 | 每条规则下实际执行的动作 |
| 规则 | 一组完整的条件组合 |

### 4.4 示例：登录结果判定表

| 规则 | 用户名存在 | 密码正确 | 账号状态正常 | 预期动作 |
| --- | --- | --- | --- | --- |
| R1 | Y | Y | Y | 登录成功，进入首页 |
| R2 | N | 不关注 | 不关注 | 提示用户不存在 |
| R3 | Y | N | Y | 提示密码错误 |
| R4 | Y | Y | N | 提示账号不可用 |

设计时可先列出理论组合，再依据业务约束合并“不关注”项，减少冗余用例。不能为了减少数量而删除真实风险组合。

## 5. 测试用例评审

### 5.1 评审目的

- 检查需求是否被覆盖。
- 检查用例是否可执行、可判断、可追溯。
- 找出重复、遗漏、错误优先级和不合理数据。
- 在执行前尽早发现需求理解偏差，降低返工成本。

### 5.2 参与角色

- 测试：主导用例、测试点和风险说明。
- 产品：确认业务规则、流程和用户预期。
- 开发：确认实现可行性、接口约束和技术边界。
- 项目负责人/架构/运营：在高风险项目中补充范围、上线和运营视角。

### 5.3 评审清单

- 是否覆盖所有需求、页面、接口、角色和状态。
- 正常流、异常流、边界、权限、兼容性是否覆盖。
- 每条用例是否只有清晰的主要目标。
- 步骤是否能让其他人复现。
- 预期是否包括页面、提示、数据库和状态变化。
- 优先级是否体现业务风险，而不是个人偏好。
- 数据是否可准备、可重复、可清理。
- 需求变更后是否同步更新用例。

### 5.4 评审会议输出

- 评审结论：通过、修改后通过、不通过。
- 问题清单：编号、问题描述、责任人、截止时间。
- 用例版本：评审前后版本号和变更记录。
- 未决事项：需要产品或开发确认的规则。

## 6. Bug 的完整生命周期

```text
发现 -> 复现 -> 提交 -> 分派 -> 修复 -> 待回归 -> 回归通过/重开 -> 关闭
```

常见状态还包括：新建、确认、拒绝、延期、重复、无法复现、非 Bug、已解决。

### 6.1 高质量 Bug 必备信息

| 字段 | 写作要求 |
| --- | --- |
| 标题 | [模块][现象][影响]，一句话说清问题 |
| 环境 | 版本、浏览器、系统、设备、测试环境 |
| 前置条件 | 账号、权限、数据、配置 |
| 重现步骤 | 按 1、2、3 写最短稳定路径 |
| 实际结果 | 客观描述观察到的现象 |
| 预期结果 | 需求或设计应达到的结果 |
| 严重程度 | 对功能、数据、系统和用户的影响 |
| 优先级 | 修复顺序和业务时限 |
| 附件 | 截图、录屏、日志、请求响应、SQL 结果 |

### 6.2 严重程度与优先级

- **严重程度**回答“问题有多严重”。崩溃、数据丢失、资金错误通常严重程度高。
- **优先级**回答“什么时候修”。营销活动前的文案错误可能优先级高，但严重程度不一定高。

不要把严重程度和优先级混为一谈；最终等级应遵循团队定义。

### 6.3 Bug 标题示例

不推荐：`登录有问题`。

推荐：`[登录][高]连续输错密码 3 次后账号仍可继续尝试登录`。

推荐：`[订单][高]支付接口超时后刷新页面生成重复订单`。

## 7. 企业实战：从缺陷到回归

1. 用最短步骤确认问题不是环境、数据或操作错误。
2. 保存原始证据：请求、响应、日志、时间点和账号。
3. 提交 Bug 后补充影响范围和建议优先级。
4. 开发修复后确认修复版本和变更范围。
5. 先验证原问题，再执行受影响模块的回归集。
6. 若仍存在、产生新问题或只修复部分场景，重开并补充证据。
7. 关闭前记录验证版本和最终结果。

## 8. 常见错误与排查

- 只写“不能用”，没有写输入、步骤和预期。
- 一个 Bug 混入多个无关问题，开发无法定位。
- 只附截图，不附请求、日志或数据条件。
- 复现失败就直接关闭，没有确认环境和数据。
- 修复一个条件后只测原路径，没有测相邻分支。
- 通过改数据库状态来复现，却没有记录 SQL 和清理方式。

## 9. 练习与答案解析

### 练习 1：错误推断

**问题**：支付按钮被连续点击两次，最少要验证哪些结果？

**答案**：订单数量、支付流水数量、扣款次数、库存扣减次数、页面提示和最终订单状态。

**解析**：重复点击可能同时影响前端、接口幂等、支付回调、库存和订单状态，不能只看页面是否提示成功。

### 练习 2：判定表

**问题**：优惠券使用条件为“已登录、未过期、订单金额达到门槛”。应覆盖哪些规则？

**答案**：三个条件全部满足时使用成功；分别验证未登录、已过期、金额不足；同时验证多个条件同时不满足时系统提示和优先级。

**解析**：判定表先列条件，再合并不影响结果的条件。金额边界还要结合边界值测试。

### 练习 3：Bug 分级

**问题**：生产环境中用户支付成功，但订单仍显示待支付。如何处理？

**答案**：按高严重程度处理，立即记录订单号、支付流水、回调日志、时间线和影响范围，并推动止损、修复和数据补偿。

**解析**：这是资金和订单状态不一致问题，可能造成重复支付、客诉或财务对账错误。测试人员不仅要写 Bug，还要提供证据和影响分析。

### 练习 4：评审发现

**问题**：用例预期写“系统正常”，是否合格？

**答案**：不合格，应改为可观察、可判断的结果，例如“页面提示注册成功，生成唯一用户 ID，使用该账号可正常登录”。

**解析**：预期结果必须能让不同执行人员得到一致结论。

## 10. 面试高频问法

### 问：如何判断一个 Bug 是否应该提交？

答题结构：先确认需求或设计依据，再稳定复现，排除环境和数据因素，记录实际/预期结果、影响范围和证据；若需求不明确，先与产品确认，不凭个人理解下结论。

### 问：严重程度和优先级有什么区别？

严重程度描述影响大小，优先级描述修复顺序。资金错误可能两者都高；低影响但临近发布的阻塞问题可能优先级高。

## 11. 章节总结

错误推断法负责“根据经验主动找错”，因果图和判定表负责“系统覆盖条件组合”，用例评审负责“执行前查漏补缺”，Bug 管理负责“让问题可定位、可修复、可验证”。

## 12. 动手任务

- 为登录、注册、下单各设计一张判定表。
- 从历史项目中整理 10 个 Bug，按输入、流程、权限、数据、并发和环境分类。
- 写 3 条高质量 Bug，并为每条设计回归范围。
